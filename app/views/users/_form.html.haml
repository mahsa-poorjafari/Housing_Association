= form_for @user || @user = User.new do |f|
  - if @user.errors.any?
    #error_explanation     
      %ul
        - @user.errors.each do |attr, msg|
          %li= msg

  .form-group
    = f.label "نام:"
    = f.text_field :name
  .form-group
    = f.label "نام خانوادگی"
    = f.text_field :last_name
  .form-group
    = f.label "شماره ملی"
    = f.text_field :national_code
  .form-group
    = f.label "تاریخ تولد:"
    = f.date_select :birthdate
  .form-group
    = f.label "تلفن ثابت"
    = f.text_field :phone
  .form-group
    = f.label "تلفن همراه"
    = f.text_field :mobile    
  .field
    = f.label "آدرس"
    = f.text_area :address
  .actions
    = f.submit 'ثبت'
